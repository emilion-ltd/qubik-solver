{
  "name": "cube-solver-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "test": "node ../tests/core.cjs && node --test ../tests/*.test.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
